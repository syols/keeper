# default.yml

server:
  address:
    host: 0.0.0.0
    port: 3200
  database: "postgres://postgres:postgres@localhost/postgres?sslmode=disable"
  sign: "some_sign"

client:
  address:
    host: 0.0.0.0
    port: 3200
  database: "postgres://postgres:postgres@localhost/template1?sslmode=disable"
