# default.yml

server:
  address:
    host: 0.0.0.0
    port: 3200
  database: "postgres://postgres:postgres@localhost/server?sslmode=disable"
  sign: "some_sign"

client:
  address:
    host: 0.0.0.0
    port: 3200
  database: "postgres://postgres:postgres@localhost/client?sslmode=disable"
